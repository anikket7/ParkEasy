# Backend Contribution Report

This document outlines the backend contributions of the 3-member team for the Parking System project.

## 👤 Team Member 1: [Name]
**Focus: Core Infrastructure & Authentication**
- **Server Setup**: Initialized the Node.js/Express.js server, configured Middleware (CORS, Parser), and set up the directory structure.
- **Database Connection**: Configured MongoDB connection logic using Mongoose and environment variable management (.env).
- **Authentication System**: Implemented `routes/auth.js` for secure User Signup and Login using JWT (JSON Web Tokens) and bcrypt for password hashing.
- **User Model**: Designed the `User.js` schema with role-based access control (Admin vs User) and validation hooks.

## 👤 Team Member 2: [Name]
**Focus: Parking Management & Data Seeding**
- **Parking Spot APIs**: Developed `routes/parking.js` (CRUD operations) to Create, Read, Update, and Delete parking spots.
- **Parking Model**: Designed the `ParkingSpot.js` schema to handle simple and complex data (pricing, features, capacity).
- **Database Seeding**: Created the `seed.js` script to automate database reset, creating the default Admin account and initial parking spots for easy deployment.
- **Availability Logic**: Implemented logic to calculate available spots and filter data for the frontend.

## 👤 Team Member 3: [Name]
**Focus: Booking System & Admin Features**
- **Booking Logic**: Implemented the booking workflow in `routes/parking.js` (Book/Release endpoints), handling concurrent booking validation and status updates.
- **Booking Model**: Designed `BookingHistory.js` to track active vs. historical bookings, storing timestamps, pricing, and user references.
- **Admin Endpoints**: Developed specialized endpoints for the Admin Dashboard (`/bookings/all`, `/user/all`) to fetch aggregated data for reporting.
- **Revenue Calculation**: Implemented backend logic to support revenue tracking and booking history retrieval.
